/**
 * @param {string} s
 * @return {number}
 */
var titleToNumber = function(s) {
  let colNum = 0
  
  for (let i = 0; i < s.length; i++) {
    colNum = colNum * 26 + (s.charCodeAt(i) - 64)
  }
  
  return colNum
};
​
​
​
